# Database connection string for PostgreSQL (Managed Cloud on Railway)
# Note: Strings starting with 'postgres://' are automatically formatted to 'postgresql://' by db.py
DATABASE_URL=postgresql://postgres:password@localhost:5432/wb_tracker

# Master password gateway for the Streamlit dashboard (defaults to admin123 if not set)
DASHBOARD_PASSWORD=admin123
